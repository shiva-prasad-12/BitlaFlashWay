<%= form_with(model: web_scrapping, local: true) do |form| %>
  <% if web_scrapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_scrapping.errors.count, "error") %> prohibited this web_scrapping from being saved:</h2>

      <ul>
        <% web_scrapping.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
